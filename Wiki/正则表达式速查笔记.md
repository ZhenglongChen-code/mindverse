---
tags: [regex, programming, cheatsheet]
createTime: 2026-04-10 11:15:00
updateTime: 2026-04-10 11:15:00
source: manual_input
category: programming/regex
---

# 正则表达式速查笔记

## 核心定义
正则表达式是一种用于匹配、查找和替换文本中特定模式的强大工具。

## 字符类
- `\d`：匹配任意数字，等价于 `[0-9]`
- `\w`：匹配字母、数字、下划线，等价于 `[A-Za-z0-9_]`
- `\s`：匹配空白字符（空格、制表符、换行符）
- `.`：匹配除换行符外的任意单个字符

## 量词
- `*`：匹配前一个元素零次或多次
- `+`：匹配前一个元素一次或多次
- `?`：匹配前一个元素零次或一次
- `{n}`：精确匹配前一个元素 `n` 次
- `{n,m}`：匹配前一个元素 `n` 到 `m` 次

## 常用模式示例
- **邮箱验证**：`^[\w.-]+@[\w.-]+\.\w+$`
- **中国大陆手机号**：`^1[3-9]\d{9}$`
- **URL**：`https?://[\w.-]+(?:/[\w./-]*)?`

## 零宽断言（高级）
- **正向先行断言** `(?=pattern)`：匹配后面跟着 `pattern` 的位置
- **负向先行断言** `(?!pattern)`：匹配后面不跟着 `pattern` 的位置
- **正向后行断言** `(?<=pattern)`：匹配前面是 `pattern` 的位置
- **负向后行断言** `(?<!pattern)`：匹配前面不是 `pattern` 的位置

## 重要说明
- **JavaScript 限制**：JavaScript 正则引擎不支持后行断言（`(?<=...)` 和 `(?<!...)`）。

## 相关链接
- [[编程]]
- [[文本处理]]
- [[JavaScript]]